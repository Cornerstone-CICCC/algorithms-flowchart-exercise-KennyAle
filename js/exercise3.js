// Write an algorithm and draw a flowchart that will read three numbers and prints the value of the largest number.

// > Given that the three numbers are not equal to each other)

let a = 12
let b = 2
let c = 3

if (a > b && a > c) {
    console.log(`A is the GOAT with a value of ${a}`)
} else if (b > a && b > c) {
    console.log(`B is the GOAT with a value of ${b}`)
} else {
    console.log(`C is the GOAT with a value of ${c}`)
}